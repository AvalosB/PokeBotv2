services:
  bot:
    build: .
    container_name: pokebot
    restart: unless-stopped
    environment:
      - DISCORD_BOT_TOKEN
      - DB_HOST
      - DB_PORT
      - DB_USERNAME
      - DB_PASSWORD
      - DB_NAME
